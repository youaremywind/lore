优先级是重要性信号，不是让所有记忆强制出现的开关。

实用规则：

| Priority | 用途 |
| --- | --- |
| `0` | 核心身份、强约束、极少变化的最高级规则 |
| `1` | 高频且重要的偏好、项目约束或工作流 |
| `2` | 普通长期记忆 |
| `3+` | 背景信息、低频参考或历史细节 |

不要把所有记忆都设为 `0` 或 `1`。优先级膨胀会让召回变吵，也会削弱真正重要记忆的信号。
